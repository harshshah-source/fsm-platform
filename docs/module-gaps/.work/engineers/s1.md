# S1 — engineers — expected yardstick

Persona: **Zonal Manager**, SE Management, all day, primary. (Ravi/SE touches this module only
via mobile availability+leave — one capability (C8) carries his side; he does not live here.)

SPINE.md: absent at run time. Derived cross-module edges from CONTEXT.md + PRD flows directly,
per S1 stage instructions (do not wait for cartographer).

## Goals (3)
1. Know who's reachable right now (Activity Status) for intra-day calls.
2. Keep roster fit to work: availability state correct, leave approved, vans stocked.
3. Plan plant-visit intent ahead via SE Planner grid.

## Capabilities — see s1.json for full src/crit/why
C1 SE Activity Status board — persona — 5
C2 Set/override SE Availability — persona — 5
C3 Approve/deny Leave Requests — persona — 4
C4 SE Planner grid — persona — 4
C5 Van Stock / Common Kit completeness view — role — 4
C6 Coverage-type & Territory visibility — role — 3
C7 Availability audit trail (set_by_role) — role — 3
C8 Mobile SOFT_UNAVAILABLE + Leave submission (SE-side) — role — 4

anchorCheck: **pass** (srcMix persona 4 / role 4 / spine 0 / prd 0 — no capability leaned on PRD
alone, all traced to persona need or the ZM's operational role).

## Scope note (boundary disagreement — recorded, not resolved)
PRD Flow 10 (SE Management) bundles SE list + Planner as one page flow, matching the four admin
routes given (`/engineers`, `/engineers/manage`, `/engineers/planner`, `/leave-requests`) — treated
as one bounded context here.

PRD Flow 5 (Component-Blocked Queue) is Ticket-centric and ZM-read-only; it shares the Common-Kit
domain rule with this module's Van Stock completeness view (C5, SE-centric) but is a **different
module** — do not fold Component-Blocked Queue into `engineers`.

Mobile availability/leave screens are in-scope per CLAUDE.md's surfacing rule (admin+mobile = one
vertical slice), even though the chosen persona (ZM) doesn't hold the phone — C8 is the seam that
represents Ravi's side without splitting the persona.

## Sources read
- PRD-fsm-admin-dashboard.md lines 136-146 (ZM SE Management stories), 259-263 (SE Availability &
  Leave), 347-478 (page flows, esp. Flow 10 SE Management, Flow 5 Component-Blocked Queue for the
  boundary note).
- CONTEXT.md: SE coverage types (~L34-65), Common Kit/Van Stock (~L268-295), SE_AVAILABILITY model
  + SE Activity Status derivation (~L329-364, 504), SE Activity Ping / OFFLINE threshold (~L446-456),
  Leave Request + no-self-approve rule (~L476-483), coverage precedence Decision (~L590-594, 689-695).
