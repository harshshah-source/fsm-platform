# S1 · EXPECTED — intraday

Read (read-ban respected, no source): PRD-fsm-admin-dashboard.md:111-120,225-231,501-664 ·
CONTEXT.md (Decisions §16 superseded, §21, Intra-day Re-plan glossary) · SPINE.md (E-18..E-20) ·
issue 268 (intent only, ignored Status line).

## Big finding — retirement, not the task-input framing

Task input's "Scope" line ("SE acceptance flow", "Mobile intra-day insertion screen", persona
candidate "Ravi ... gets interrupted mid-shift and must answer") describes the **retired** shape.
CONTEXT.md Decision §21 (#268, ratified 2026-08-20) supersedes Decision §16: CRITICAL/HIGH_CRITICAL
insertions are **assigned directly** — no offer, no Accept/Decline, no Acceptance Timeout, no retry
chain. Mobile Accept/Decline screen + `/me/intraday-insertions` endpoints are deleted (#279), not
versioned. CONTEXT.md is authority #1 per CLAUDE.md, so this yardstick is built against §21, not the
PRD lines the task pointed at. Recorded as `scopeNote` in s1.json — not resolved, per stage rule.

Consequence for persona pick: Ravi never acts inside this module's boundary any more (no
accept/decline button anywhere in the product). He only *receives* an informational notify and sees
the ticket at the top of his Day Plan — that's scheduling/notifications territory (PRD Flow 1 item
5), not intraday. Zonal Manager is the one who lives here: watches direct-assignment resolve,
handles escalations, does manual same-day edits.

## Persona

Zonal Manager. Frequency: every CRITICAL/HIGH_CRITICAL qualifying event during the shift; ad hoc for
manual same-day Day Plan edits.

## Goals (3)

1. Watch each direct-assignment resolve (who got it / escalated) — no pre-commit veto exists any more.
2. Resolve `ESCALATION_REQUIRED` (no capacity-eligible SE) via explicit manual assignment.
3. Manual same-day plan edits (add/remove/reorder) with ON_SITE conflict warning.

## Capabilities

| id | name | src | crit | why (1 line) |
|---|---|---|---|---|
| C1 | Direct-assignment resolution feed | persona | 5 | no veto step left — ZM must see the outcome right after the fact or hears it from the field first |
| C2 | SE informational notify completes the hand-off | spine | 5 | this notify IS the whole hand-off now (no accept step left to catch a silent failure); rides same broken push seam as E-05 |
| C3 | Escalation queue + explicit manual assign | spine | 5 | system never self-authorises overload — escalation is the only human catch for an unassignable CRITICAL |
| C4 | Reverse path: SE can't do a directly-assigned stop | role | 4 | no Decision names this case post-#268; nearest analog is general override/C3 — unconfirmed, flagged not asserted |
| C5 | Manual same-day edit + ON_SITE conflict warning | persona | 4 | only field-condition lever short of reassignment; missing warning can yank a ticket from a physically-present SE |

## ANCHOR-CHECK: pass

srcMix persona=2 role=1 spine=2 prd=0. Zero `prd` sources — expected, since the PRD sections this
module points at describe the retired mechanic; every capability re-derived from persona need or
SPINE edges under the current (§21) design instead of echoing PRD stories 78-81 / Flow 4.

## Scope note (unresolved, per step 1)

PRD 111-120/225-231 + App-Flow Flow 4 (541-547) = retired SE-Acceptance mechanic. Do not build
S2/S3/S4 against those lines. Boundary also drops "mobile intra-day insertion screen" from scope —
it was deleted (#279), not a gap to price.
