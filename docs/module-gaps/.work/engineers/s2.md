# S2 · ACTUAL — engineers (SE Management, Planner & Availability)

surveyor · static read only · NO BROWSER · checks 3/6/8 stay `?` unless a consumer is named at file:line

## headline

**The carrier exists. The cartographer missed it.** Two live in-process edges, neither in SPINE.md:

| edge | writer | reader | shape |
|---|---|---|---|
| availability → dispatch | `se-availability.service.ts:136` ($transaction) | `recommender.service.ts:1035`, `scheduling/candidate-query.service.ts:122` | `currentStatusMany(seIds, now)` — **live table read**, not a materialised view |
| leave → availability → dispatch | `leave-request.service.ts:96` (approve calls setAvailability) | same two readers | approval writes the window and links `availabilityId` at `:110` |
| planner intent → batch run | `se-planner.service.ts:44` | `recommender.service.ts:529` → `:646` (ADR-0022 soft bias) → `:862` trace | plant_id → planned se_ids |

Verdict: **(a)**, not (b). No stale view anywhere — `grep -rn 'materialized|REFRESH|candidate_pool'` over
`recommender/`, `scheduling/`, `schema.prisma` returns nothing. The only cache is the per-run Map at
`recommender.service.ts:531`, rebuilt each run at `:624`. An SE flipped to `ON_LEAVE` is dropped by
`candidate-readiness.ts:56` on the next run. **This is NOT a C2 broken hand-off.** → `ENG-G3` is O4
(spine-map documentation), S2, dev-process only — never in the feature backlog.

## the audit gap, judged as a working-time decision

`static.md` flagged 5 unaudited write endpoints. Judged on substance, not screens:

- `POST /leave-requests/:id/approve` + `/reject` — **P2, S3, DANGEROUS** (`ENG-G1`). Approve *borrows*
  `SE_AVAILABILITY_SET` (`se-availability.service.ts:148`, entityType `se_availability`), so the window
  is traceable but the *decision* is not. Reject writes `decidedBy`/`decisionReason`
  (`leave-request.service.ts:126-135`) with **no audit row at all** — a denied leave is unreconstructable.
- `POST /planner` + `DELETE /planner/:id` — **P2, S3, DANGEROUS** (`ENG-G2`). These writes let a
  planner-named SE outrank the top-scored candidate (`recommender.service.ts:646`). The trace stamps the
  *effect* (`plannerBias`, `:862`); nothing names who created the *cause*.
- `POST /leave-requests` (submit) — low. Submitting is a request, not a decision; folded into `ENG-G1`.

`GET` rows in `static.md` marked "no audit" are reads — not findings.

## skeleton corrections (both would have mispriced this module)

1. **"no data hook" on all four admin pages is a false negative.** The pages use
   `useEffect` + typed api clients, not react-query. `LeaveRequestsPage.tsx:3`, `PlannerPage.tsx:9-10`,
   `SeManagementDirectoryPage.tsx:3-19`, `SeManagementPage.tsx:3-11`. The "admin api client calls"
   section is empty for the same reason — `api/leaveRequests.ts`, `api/planner.ts`,
   `api/engineersAdmin.ts`, `api/engineers.ts` all exist and are imported.
2. **Skeleton missed the mobile availability screen.** It lists only `availability/availabilityDisplay.ts`
   (25 loc). The real screen is `apps/mobile/src/navigation/screens/AvailabilityScreen.tsx:28`, reachable
   at `ProfileScreen.tsx:24`; leave at `ProfileScreen.tsx:21` → `LeaveRequestScreen.tsx:53`. **C8 is not
   a C4 asymmetry.**

## absence earned, then withdrawn

`SeManagementDirectoryPage.tsx:54` offers only `DEDICATED`/`MULTI_PLANT` — looks like C5 partial scope
against s1's three coverage types. **Not a gap.** `schema.prisma:290` and
`engineer-admin.service.ts:265` (`FLOATING_USES_TERRITORY`) both say FLOATING never appears in the
coverage table; its lens is the territory module (`apps/admin/src/api/territory.ts`). Terminology +
other-module-entry-point checks both pass. Withdrawn before filing.

## real remaining gaps

- `ENG-G4` C5/S2 — no overlap or duplicate guard on leave windows. `submit()` at
  `leave-request.service.ts:61` checks engineer-exists + authz only; `schema.prisma:1514-1515` has two
  indexes and no uniqueness. Two approvals stack two `SeAvailability` rows and `:93` silently keeps only
  the latest `windowStart`.
- `ENG-G5` C5/S1 — `reason` is free text (`se-availability.service.ts:18,146`). `set_by` and
  `set_by_role` **are** written (`:144-145`) and surfaced (`:82`); only s1 C7's coded reason is missing.

## states — nothing FUNC_COMPLETE (static read, per law)

All 8 capabilities `PARTIAL`. C2 and C4 are the strongest (8 of 9 checks pass or are named); C3 is the
weakest of the well-built ones (5 and 9 fail). Nothing is `NOT_STARTED`, `BACKEND_ONLY`, `UI_ONLY`,
`BROKEN` or `OBSOLETE` — every capability has both layers wired, which is the real finding here: this
module is far healthier than a spine map with zero edges implies.

## walkability

Walkable at S3: all 8. The five hypotheses are the ones that move a price — two carrier-works
(H1 availability→run, H2 planner→run), one persist on mobile (H3), one perms (H4, the `zoneId === null`
unscoped-ZM promotion at `leave-request.service.ts:190`), one persist/asymmetry on admin van stock (H5,
`vanStock` returned at `engineers-query.service.ts:182` with no admin consumer found).
