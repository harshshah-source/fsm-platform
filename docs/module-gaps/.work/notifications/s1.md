# S1 · EXPECTED — notifications

Read: stage file, PRD lines 211-216 + 725-730, SPINE.md (grep, edges E-04/E-05/E-19/E-22/E-24), CONTEXT.md (grep, Decision §15 acted_as_role, freshness banner). spineAvailable: true.

## Persona
**Ravi, Service Engineer** — continuous through a full day of stops, mobile only, app closed between jobs.

## Goals (3)
1. See every SE-relevant event without polling.
2. Actually get it on his phone with app closed (not just an unread in-app row).
3. Read / read-all triage so the list stays usable across a day.

## Scope note
PRD groups Notifications + Audit under one "All Roles" heading. Two different jobs share it:
notifications = CARRIER (moves work state to SE, taxonomy C2), audit trail = LEDGER (proves what
happened, taxonomy P2/O1). Ravi lives in the carrier half daily; audit trail is Operations Head's
occasional "who changed this" concern. Kept both in one yardstick per PRD's own boundary, capabilities
tagged so the split stays visible for later stages.

## Capabilities

| id | name | src | crit | why |
|----|------|-----|------|-----|
| C1 | in-app list, all SE-relevant events | persona | 5 | no signal anything changed without opening app |
| C2 | push/SMS/WhatsApp to SE phone, app closed | persona | 5 | in-app-only row he never opens = day plan silently wrong |
| C3 | read / read-all state | persona | 3 | unread pile buries the urgent SLA warning |
| C4 | WhatsApp Confirmation first-class for SE Acceptance | prd | 4 | Acceptance Timeout fires falsely without guaranteed channel |
| C5 | full Ticket audit trail incl. closure_type+reason | role | 4 | no record to arbitrate disputes / incident review |
| C6 | acted_as_role stamped on delegated-scope audit entries | role | 4 | Decision §15: unstamped entry misattributes CSM-acting-as-ZM actions |
| C7 | Snapshot freshness / FAILED alert as notification | role | 3 | stale data mistaken for real fleet-health improvement |

## Anchor-check
PASS — srcMix persona 3, role 3, spine 0, prd 1. Not all-prd.

## Stack constraint (noted, not resolved here)
No Redis/BullMQ/S3. Durable delivery (C2) must be a table-backed outbox + cron drain, not a queue
product — yardstick above states the capability regardless; how it's built is a later-stage concern.
