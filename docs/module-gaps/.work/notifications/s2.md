# S2 · ACTUAL — notifications (Notifications & Audit Trail)

Two clusters, scored apart. CARRIER = C1-C4 (moves work state to Ravi). LEDGER = C5-C7 (proves what
happened). No browser; checks 3/6 stay `?`. Nothing marked FUNC_COMPLETE.

## CARRIER — in-app half is real, delivery half is a log line
| cap | crit | state | why |
|---|---|---|---|
| C1 list, all SE events | 5 | PARTIAL | 12 types from 14 call sites; SLA warning / snapshot / component approval absent |
| C2 push when app closed | 5 | BACKEND_ONLY | gateway returns `UNAVAILABLE`, no adapter, no mobile token client |
| C3 read / read-all | 3 | PARTIAL | backend + mobile screen present; persist unwalked |
| C4 WhatsApp SE-Acceptance | 4 | BACKEND_ONLY | branch coded, zero production caller |

**E-05 settled from source, no hypothesis spent.** `notification-channel.gateway.ts:37` `deliver()`
returns `'UNAVAILABLE'` at `:39` unconditionally; `LoggingChannelGateway` is the only implementation;
`grep firebase|fcm|apns|expo-notifications` over backend+mobile returns deferral comments only.
**No delivery mechanism beyond an in-app list the app must be open to poll.** `BACKEND_ONLY` not
NOT_STARTED because two real pieces exist: `device_tokens` (schema.prisma:225, upsert
device-token.service.ts:15) and `DayPlanNotificationOutbox` (schema.prisma:2899, drained
business-sweep-scheduler.service.ts:275).

The outbox is the sharp finding, not its absence: the table exists and drains *into the UNAVAILABLE
gateway* — a durable queue in front of a dead exit. HITL-blocked seam is CLAUDE.md-legal; the SE not
learning is still S4. And it covers **one** path — the other 12 `notify()` sites
(intraday-insertion.service.ts:343, cross-zone-escalation.service.ts:300, recovery-notifier.ts:76,
install-notifier.ts:54, stranded-work-escalation.service.ts:123, bulk-unassign.service.ts:324) fire
post-commit, best-effort, no retry, no row — a crash loses the notice. NOTIF-02, the one DANGEROUS row.
C4 is a dead branch: `grep SE_ACCEPTANCE` (non-spec) hits only notification.service.ts:11,14,70,158;
it records `ATTEMPTED` not a false `SENT` (:159-164), so missing channel, not falsified record.

**Guards.** `static.md`'s 3 unguarded endpoints are scoped by JWT subject, not role — no gap.
Class-level `@UseGuards(AuthGuard, RoleGuard)` at controller:14; every handler passes `user.user_id`
into a service whose every where-clause carries `recipientUserId` (notification.service.ts:92,117,
122,128). `@Roles` would be wrong here. Cross-user read is H1.

## LEDGER — 97 write sites, one read path, no screen
| cap | crit | state | why |
|---|---|---|---|
| C5 per-ticket trail | 4 | BACKEND_ONLY | endpoint merges events+logs correctly; no admin UI at all |
| C6 acted_as_role | 4 | PARTIAL | written uniformly and in-tx; readable only inside one ticket |
| C7 snapshot FAILED alert | 3 | NOT_STARTED | no notify() in snapshots, no SNAPSHOT_* type |

OH answers "who changed this and in whose scope" **only with the ticket UUID in hand, and only by curl.**

- Write side good: `audit.service.ts:60` wraps mutation + audit row in one interactive transaction (no
  unaudited window); `auditActor()` :30 stamps `actedAsRole`/`actingZone`; column + index exist
  (schema.prisma:1724, :1733).
- Read side is one endpoint, filtered `entityType='ticket'` (audit-trail.service.ts:50); ZM
  zone-scoping correct (:46). **No admin consumer** — `grep audit-trail|auditTrail` over
  `apps/admin/src` = 0, skeleton.md's admin-UI and api-client sections empty. NOTIF-03 (C4), H2.
- **No query by actor, acted_as_role, acting zone or date.** The index `[actedAsRole, actingZone,
  createdAt]` is exactly the CSM-backup question and nothing reads it; audit from devices, engineers,
  leave, schedules, vouchers is unreachable. NOTIF-04 — "audit table nobody can query": O1, P2-adjacent.

C6 stays PARTIAL on a second doubt (H5): `acting-context.ts:30` is the only place a non-null
`actedAsRole` is derived, while cross-zone.controller.ts:113, devices.controller.ts:131,
engineers.controller.ts:254, leave-request.controller.ts:69,85,103 pass literal `null`. If those win
in a real session, C6 is a facade not a half-build — that changes the price.

## Shared gap, owned here — NOTIF-01 (P11, priced once)

Two evidence lines, one finding. (1) this survey: gateway `:37`→`:39` returns `UNAVAILABLE`, only
impl, no FCM/APNs/expo dep, #264 outbox drains into it. (2) scheduling surveyor, independently: same
gateway `:37`, and `HomeScreen.tsx` has no poll, so in-app is the only path. `notifications` owns the price. Blocked downstream: **scheduling** (E-05), **intraday** (E-19),
**inventory** (E-24) — they reference `NOTIF-01` and must not re-price. Fix scope in the row for S5:
bind a real adapter at `notifications.module.ts:10`, register tokens from mobile (NOTIF-07 folds in),
route the outbox drain through it.

## O4 · dev-process — scanner columns are not evidence (not in the feature backlog)
Coordinator correction: `static.md`'s **audit** column follows one delegation hop and under-detects
where the write sits in a shared helper (false 11/11 scheduling, 8/10 tickets); **guard** can be off by
one decorator. A "yes" is trustworthy, a "no" is not. Both re-checked at `file:line` here: guard's 3
`none` endpoints → **false alarm, no gap filed** (class-level guard, controller:14); audit's 3
no-audit writes → **true, and correctly not a gap** (notification.service.ts:115-130 are plain
`updateMany`, no helper hop; per-user triage is not a money/stock/state write, so check 9 is `NA`).
No finding here rests on an unverified scanner cell.

## Could not map / notes for S3
Nothing unmapped — all 7 S1 capabilities hit source. Walk order: H4 (cheap, mobile), H1 (two SE tokens), H5 (CSM with `X-Acting-As-Zone`, then read
`audit_logs`), H2/H3 (confirm the two zero-hit greps at runtime). H5 is the one that moves money.
