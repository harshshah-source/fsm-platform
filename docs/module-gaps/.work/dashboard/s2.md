# S2 · ACTUAL — dashboard (Zone Dashboard Home)
E2 static only. No browser. Checks 3 / 6 / 8 stay `?` on every capability.

| cap | crit | state | one-line |
|---|---|---|---|
| C1 Action Required | 5 | PARTIAL | panel renders, 4 of 9 sources wired, no card is clickable |
| C2 Zone Overview | 5 | PARTIAL | buckets real, Trend column structurally dead |
| C3 Company/Plant | 4 | PARTIAL | deepest surface here; drill-down + 3 exports |
| C4 Critical Work Queue | 5 | BACKEND_ONLY | ZM sees none; CSM sees a flattened read-only list |
| C5 Snapshot freshness | 4 | PARTIAL | global banner honest, dashboard badge lies |
| C6 Operating mode | 3 | BACKEND_ONLY | two finished components, mounted nowhere |
| C7 Activity trend | 2 | PARTIAL | mounted in all three variants |
| C8 Excel export | 3 | PARTIAL | Company/Plant yes, Zone Overview + Scorecard no |
| C9 Role lens | 4 | PARTIAL | lens exists, contents inverted vs persona |

## C9 — a real lens, NOT taxonomy C6

`ManagerDashboard.tsx:157-161` branches to three genuinely different bodies; `actingZone` in the effect
deps (`:105`); prior-scope rows cleared before refetch (`:53-61`); WM diverted before the manager
endpoints fire (`DashboardHome.tsx:13`). Not "same tables, different row count". The finding is that
lens **contents are inverted**: the ZM — crit-5 persona — is the only role without a critical queue
(`ZmDashboard.tsx:48-56`); CSM/OH the only roles without the Action Required panel
(`CentralDashboard.tsx:19`, `OpsHeadDashboard.tsx:42`). Different *rows* per role = check 6, `?` (H1).

## C1 — rows real, hand-off missing, missing half already written

`dashboard.service.ts:889-901`: four bespoke `$queryRaw` counts (VUR open, verification_runs
FAILED_VERIFICATION, failure_cycles paused 7d+, RECOVERY stalled 14d+). Other five of nine keys
(`:292-306`) forced `count:0 available:false`, rendering literal "coming soon"
(`ActionRequiredPanel.tsx:45-47`). Every card is an `<li><Card>` with **no `onClick`, no `navigate`**
(`:27-52`) — s1 defines the capability as "surfacing + one-click routing", routing absent. C2, not a
display gap. But routing exists **one module over**: `dispatch/console/AttentionBand.tsx:3` reads the
same `apiActionRequired`; `:30-37` `CARD_DESTINATION` maps 7 of 9 keys to route + verb, rendered
`<Link to={dest.to}>` at `:188`; its docblock (`:21`) states the very rule the dashboard breaks —
*"a live count with no destination page says…"*. So DASH-G02 = consume an existing map, not design one
→ UI-only price; and 3 of the 5 unwired keys already have destinations, making DASH-G05 backend-count
work only. ZM reachability of those routes: DASH-H4.

## C4 — queue cannot dispatch, and the ZM cannot even see it

Backend groups by company/plant (`dashboard.service.ts:803`). `ZmDashboard` never destructures
`critical` / `engineers` / `onAssigned`, renders no queue (`:48-56`, `:99-148`). Only
`CentralDashboard.tsx:91` renders anything, and `EscalationQueueList.tsx:16-33` **flattens the grouping
away** into severity-sorted rows — no assign control, no suggested SE, no link out (`:60-79`).
`ManagerDashboard.tsx:95-97,152` fetches `apiZoneEngineers()` + builds `onAssigned`; both cross
`DashboardData`, consumed by **zero** components. C7 on top of C4.

## C5 — dashboard end only (P11)

Global banner honest: mounted everywhere (`AppRoutes.tsx:62`), real `dataAsOf`
(`SnapshotBanner.tsx:111-116`), degrades to "No successful snapshot yet" (`:117`), `partialDataAsOf`
(`:119-123`), RUNNING past `STUCK_AFTER_MS` = stuck (`:29`). Then `ZmDashboard.tsx:106-108` and
`CentralDashboard.tsx:56-58` paint a **hardcoded** green `Snapshot Healthy` badge with nothing behind
it — red banner and green badge can appear together. O3, reach 5, one-line fix. DASH-G04 prices those
two badges only; whether `dataAsOf` tracks a *successful* run or the last attempt is the producing end,
ingestion surveyor's, my hypothesis on it withdrawn so it is not double-priced.

## Structural

`dashboard.service.ts:454` assigns `trendPctVsPrevDay: null`, the **only** assignment of that field in
the backend → C2's "trend vs yesterday" column can never render a number
(`ZoneOverviewTable.tsx:145-151`). Same file's docblock (`:12-19`) claims "a CSV export" + "the export
button are all preserved"; no `exportFile`/`TableDownloadButton` import (`:1-7`), none in
`ScorecardTable.tsx` — scoped claim: rows are live (it does import `api/dashboard`), only the export
control is fictional, copy `CompanyPlantTable.tsx:320-345`. Separately `ZoneOperatingModeCard/Table.tsx`
are complete, self-fetching, plain-language (`utils/operatingModeCopy.ts`) over a guarded endpoint
(`operating-mode.controller.ts:26`) and imported by no `.tsx` outside themselves — C6's cost is a mount.

## Scanner corrections — O4, dev-process, NOT in the feature backlog

| column | verdict | measured here | effect on this brief |
|---|---|---|---|
| `skeleton.md` "no data hook" | FALSE NEGATIVE, all 16 files | **11 of 16 import `apps/admin/src/api/`** (ManagerDashboard 3, CompanyPlantTable 3, WarehouseDashboard 3, ZmDashboard 2, ScorecardTable 2, six at 1); the other 5 are presentational children or a router, where 0 is correct. `skeleton.md:50` claims zero api calls — `ManagerDashboard.tsx:62-97` fires eight. Cause: scan expects react-query, repo uses `useEffect` + typed clients | **none** — every C3/C4 here was taken from the file's own import block, not this column |
| `static.md` status-write | FALSE POSITIVE on all 3 dashboard GETs (`static.md:8-10`) | `dashboard.service.ts` = **22 `$queryRaw`, zero `update`/`create`/`delete`/`upsert`/`executeRaw`/`$transaction`**; no `SoftStateService` import anywhere in `apps/backend/src/dashboard/`. Scanner matched `status` inside SELECT predicates (`vu.status = 'OPEN'`, `t.status NOT IN (…)`) | service is pure-read; no unaudited write on a read path. My P2 hypothesis **withdrawn as settled** |
| `static.md` audit | under-reports, one-directional | not exercised | **none** — all capabilities are read aggregations, check 9 `NA` throughout, no gap claims a missing audit |

## Not settled by code

H1 role scoping · H2 any downstream assign path · H4 ZM reachability of the seven `CARD_DESTINATION`
routes · H5 new SQL vs fan-in for the five unwired cards.
