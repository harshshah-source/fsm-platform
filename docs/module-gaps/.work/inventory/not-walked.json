{
  "items": [
    "INV-G1 arithmetic (van stock before/after a WM dispute) — blocked by INV-G8 and by an empty se_van_stock; needs the fixture named in gaps-INV-G1.md",
    "INV-G2 recovery receipt -> warehouse stock — recovery/awaiting-receipt len=0, no COLLECTED ticket to receipt",
    "INV-G4 7-day stale-request escalation — zero component requests exist in any lens, nothing old enough",
    "INV-H2 / INV-G3 ship-notification hand-off (check 8) — no request can reach SHIPPED with zero requests seeded",
    "check 3 persist for C1, C2, C3, C5, C7, C9, C10 — every inventory write path needs a seeded row this DB does not have; no WM/OH mutation attempted, to leave stock uncorrupted",
    "C6 GPS/SIM serial verification (INV-G6) — S2 NOT_STARTED, walking can only re-prove an absence",
    "browser walk of ShadowUseQueuePage / ComponentRequestsPage / mobile StockScreen — ~708k TEQ, and every one of them would render the same empty lists the API returned",
    "disclosure sweep on inventory admin screens — no browser walk taken this stage"
  ]
}
