# 49 — Device `deal_type` column + Operations-Head manual tagging

Status: ready-for-agent
Type: AFK
Origin: #48 triage (2026-06-21) — chose Option B (dedicated slice before #35) over retrofitting done 04/05.

> *This was generated by AI during triage.*

## What to build

The `deal_type` commercial attribute that #35 (Non-Operational dual-confirmation) depends on, plus the
Operations-Head manual-tag surface — landed as its own slice immediately before #35 so the column and
its first consumer stay adjacent (CONTEXT.md "Deal Type"; ADR-0014).

- **Column:** `device.deal_type` enum `RECURRING | ONE_TIME` (nullable — unknown until sourced).
  Conceptually sourced from CRM/SAP; in v1 there is no integration, so it is populated by the manual
  tag (below) with `NULL` = untagged. (Place on the `device` master from Issue 05, not `device_states`
  — it is a stable attribute, not a per-snapshot value.)
- **Manual tag endpoint:** Operations-Head-only `PATCH`/`POST` to set a device's `deal_type`, audited
  (`AuditService.withAudit`). Operations Head is the configurator (CONTEXT.md roles).
- **Admin UI:** a minimal tag control (Device admin / detail) — may defer to the Device Detail page
  (#44) if that lands first; the endpoint is the hard requirement here.

## Acceptance criteria

- [ ] `device.deal_type` enum column added (RECURRING | ONE_TIME, nullable) + migration
- [ ] Operations-Head-only manual-tag endpoint sets `deal_type`, audited; other roles 403
- [ ] `deal_type` readable wherever #35 needs it (device read path)
- [ ] INDEX + #35 "Blocked by" reflect that #35 depends on this slice

## Blocked by

- #05 (device master table)
- #02 (Operations-Head config/role surface)
