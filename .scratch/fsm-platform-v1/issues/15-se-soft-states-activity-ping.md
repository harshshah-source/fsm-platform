# 15 — SE soft states + activity ping + derived Activity Status

Status: done
Type: AFK
Progress: docs/progress/15-se-soft-states-activity-ping.md — all 7 ACs green (backend +42 tests/+11 files, migration 21). Mobile UI deferred to mobile ticket screens. 2026-06-23.

## What to build

The SE soft-state lifecycle and the derived SE Activity Status. Soft states VIEWED → ON_SITE → TROUBLESHOOT_STARTED on a Ticket. ON_SITE auto-updates when a deliberate app action places the SE inside the Plant/vehicle geofence (`onsite_source = AUTO_GEOFENCE`), with a manual ON_SITE tap fallback (`onsite_source = MANUAL`, audited) — no separate confirmation screen. **SE Activity Ping**: any SE-initiated app action updates `ENGINEER_MASTER.last_activity_at` (no background timer; background processes must not ping; pings never auto-clear soft states). Soft-state rules: VIEWED clears on a configurable timeout (default 1.5 h, audit-retained); ON_SITE and TROUBLESHOOT_STARTED never expire by time and are resolved only by explicit events; long-running ON_SITE/TROUBLESHOOT_STARTED trigger configurable stale-work warnings to the ZM without clearing state. **SE Activity Status** (AVAILABLE / ON_SITE / BUSY / SHIFT_ENDING / OFFLINE) is computed at render time from SE_AVAILABILITY + soft states + `last_activity_at` — never stored. OFFLINE means "app not recently used", not "SE absent".

> *This was generated by AI during triage (2026-06-21).* **Cross-reference:** Issue 13a built the ZM
> override ON_SITE-conflict check behind a seam — `apps/backend/src/scheduling/soft-state-conflict.ts`
> (`SoftStateConflictPort`, default `NoConflictSoftStatePort`). When this issue lands `soft_states`,
> provide a Postgres-backed adapter (active ON_SITE/TROUBLESHOOT_STARTED per ticket) and bind it to the
> `SOFT_STATE_CONFLICT` token in `scheduling.module.ts` so override conflicts become real.

## Acceptance criteria

- [x] VIEWED → ON_SITE → TROUBLESHOOT_STARTED transitions enforced; invalid transitions rejected
- [x] ON_SITE auto-set from geofenced action (`AUTO_GEOFENCE`) with audited manual fallback (`MANUAL`)
- [x] SE-initiated actions update `last_activity_at`; background processes do not; pings never clear soft states
- [x] VIEWED clears on configurable timeout; ON_SITE / TROUBLESHOOT_STARTED do not expire by time
- [x] Stale-work warning fires at configured threshold without clearing state
- [x] Activity Status derived at render time (never stored); OFFLINE = "app not recently used"
- [x] Real `SoftStateConflictPort` adapter wired to the `SOFT_STATE_CONFLICT` token (replaces the 13a no-conflict seam)

## Blocked by

- #11
