# standing rules — carried across every module in this sweep

One line, imperative, with provenance. Later modules MUST read the last 15 lines before judging.

- INDEX.md / issue `Status:` lines are hypotheses, never evidence — the tracker admits four stale-in-place failures  [CLAUDE.md + INDEX.md convention block, standing law]
- backend without its admin page or SE screen is C4 asymmetry, not "done"  [CLAUDE.md surfacing rule, standing law]
- no Redis/BullMQ/S3 in this stack: a "queue" is a table or a cron sweep; durable async with no outbox table is the finding  [SYSTEM-STATE, standing law]
- a receiver that must be told the item number is MANUAL, not a hand-off  [spine, standing order]
- intra-day SE accept/decline/timeout/retry is RETIRED (CONTEXT.md Decision §21, #268 2026-08-20, superseding §16); direct assignment only, mobile screen + endpoints deleted by #279. PRD :111-120 / :225-231 / :541-547 are STALE on this. Never file "SE cannot decline a stop" as a gap — it is standing law. Spine edge E-20 must be re-read this way  [intraday, standing law, policy P10]
- CONTEXT.md outranks the PRD wherever they disagree (CLAUDE.md authority order); when they do, record the diff, then judge against CONTEXT.md  [standing law]
- probe a write permission with an EMPTY body: 403 proves the guard, 400 proves the role got past it, and neither mutates a shared dial — this is how check 6 is settled on a live backend other walkers are using  [admin-config, standing order]
- audit written is not audit readable: after checking the writer passes `metadata`, check the READER projects it. `ops-explorer/dataset-registry.ts:2489` AUDIT_LOGS omits the metadata column, so correctly-written before/after values are invisible app-wide  [admin-config, standing law]

## Scanner defects — O4, never in the feature backlog (found by surveyors, 2026-09-02)
- `static.md` AUDIT column follows only ONE delegation hop; audit in a shared private helper reads as "no audit". Measured false-positive rate: scheduling 11/11, tickets 8/10, vouchers 0/0. Error is one-directional — under-detects, never invents — so "yes" is trustworthy, "no" must be verified at `file:line`  [scheduling+tickets, standing law]
- `static.md` GUARD column can be off by one decorator (scheduling: MANAGER_ROLES read as SERVICE_ENGINEER; inventory: WRITE_ROLES read as READ_ROLES). Read the decorator in source  [inventory, standing law]
- `skeleton.md` "no data hook" is a FALSE NEGATIVE — admin pages use `useEffect` + typed clients from `apps/admin/src/api/`, not react-query. Never infer a facade from it  [engineers+inventory+dashboard, standing law]
- `api-walk` prints only the FIRST 2 array items, so it cannot settle a whole-payload scoping claim — write a 20-line aggregator against the same login flow and summarise `[...new Set(rows.map(r=>r.zoneId))]`. Also pass paths with NO leading slash and prepend it inside your own script: Git-Bash rewrites a bare `/dashboard/x` argument into `C:/Users/.../Git/dashboard/x` and returns a 404 that reads like a missing route  [dashboard, standing law]
- `skeleton.md` misses mobile screens outside a module's declared sourceDirs — real screens live in `apps/mobile/src/navigation/screens/`. Never file C4 asymmetry on a missing SE screen without grepping there first  [inventory+engineers, standing law]
- **The SE persona is a shell — no `engineer_master` row.** `auth-fixture-seed.ts:74` seeds a `users` row for `se.north@fsm.test` (`user_id 22222222-2222-2222-2222-222222222222`, role SERVICE_ENGINEER) but no matching `engineer_master`, while `schema.prisma:239` requires `engineerId == the SE's user_id`. So every SE-side write that looks up the engineer fails — `POST /api/vouchers` → `400 SE_NOT_FOUND` — and every SE list reads empty (`/me/tickets` 0, `/me/vouchers` 0). The 15 real engineers from `GET /engineers` have no password, so no JWT can be minted for them, and `POST /engineers` mints a *new* uuid with no credential. **No SE-side persist (check 3) is walkable in any module until one fixture row exists**: `engineer_id 22222222-2222-2222-2222-222222222222`, `zone_id 1`, `coverage_type DEDICATED`, `daily_capacity 5`. This is O4 (dev-process), never a feature-backlog item. Report it as `UNTESTABLE`/`NEEDS-VERIFY` naming this fixture — never infer a pass, and never re-diagnose it  [vouchers, standing law]
- **A 404 carrying the service's own error code is a check-6 PASS, not a failure.** `POST /vouchers/<uuid>/review` as OH → `404 VOUCHER_NOT_FOUND` proves the RoleGuard cleared and the service body ran; the wrong role on the same route → bare `403 Forbidden`. Use the 404-vs-403 contrast to settle role membership at runtime (policy P5) on any module whose tables are empty — it needs no data at all  [vouchers, standing law]

## Shared gaps — ONE id, ONE price, referenced by both sides (policy P11)
- **Push adapter.** `notification-channel.gateway.ts:37` returns `'UNAVAILABLE'` unconditionally; `HomeScreen.tsx` has no polling; outbox producer/drain is solid. OWNED AND PRICED BY `notifications`. scheduling (C8 + SE halves of C7/C9, edges E-05 BROKEN / E-24 PARTIAL), intraday (C2), inventory (INV-G3) all bottom out here and must reference, never re-price  [scheduling+notifications, standing order]
- **SE Planner** lives in `engineers` (schema.prisma:762, AppRoutes.tsx:365) — scheduling's C6 is not scheduling's to price  [scheduling, standing order]
- **Manual assignment fallback** lives in `intraday` (intraday-insertion.controller.ts:71,79) — scheduling's C10 is not scheduling's to price  [scheduling, standing order]
- **Report/dashboard staleness** has two ends: `ingestion` owns the producing fix, `reports`/`dashboard` own only what their own screen renders and whether it degrades honestly  [reports+dashboard, standing order]

- **No producer for the consumption ledger.** `TroubleshootSubmitRequest` carries no `consumedComponents` field (`packages/shared/src/index.ts:468`, #101) and `troubleshoot.controller.ts:52-70` never sets it, so both loops at `troubleshoot-submission.service.ts:296,330` are dead over HTTP: `seVanStock` is never decremented and `inventoryTransaction` is never written. OWNED AND PRICED BY `inventory` as INV-G8. Shadow use, PRE_VERIFICATION, the verification rollback, van-stock depletion and the Component-Blocked queue all bottom out here — reference it, never re-price it  [inventory, standing order]
- No `psql` and no `docker` on PATH in this environment: `api-walk` is the ONLY instrument that can reach data. Never plan a DB fixture you cannot apply; return `UNTESTABLE` with the fixture named instead  [inventory, standing law]
- The whole inventory data surface is EMPTY (van stock, shadow use, warehouse stock, component requests x3 lenses, component-blocked, awaiting-receipt all 0 rows, measured 2026-09-02). Check 3 (persist) is unwalkable module-wide  [inventory, standing law]

- **SUPERSEDES line 28.** The DB *is* readable — only the `psql` binary is missing. Import `pg` by absolute URL (`file:///C:/fsm-platform-backup/apps/backend/node_modules/pg/lib/index.js`) and connect to `postgresql://fsm:test123@localhost:5433/fsm`. Query snake_case tables, not Prisma model names. `UNTESTABLE` for want of a DB read is no longer honest  [auth-access, standing law]
- `api-walk.mjs` sends no custom headers. Any claim about `X-Acting-As-Zone` needs your own ~25-line fetch script (keep it in the session scratchpad, never in the repo)  [auth-access, standing law]
- **`audit_logs.acting_zone` is overloaded.** `bulk-unassign.service.ts:289,367` writes the *target* zone of the unassign into it with `acted_as_role` null; 30 of the 31 non-null `acting_zone` rows in the whole ledger are that. Never read `acting_zone` as proof of an acting context, and never price a fix that only stamps `acted_as_role`  [auth-access, standing law]
- **Acting scope narrows five controllers and nothing else** (`@CurrentScope`: dashboard, reports, batches, dispatch-today, schedules). `GET /tickets`, `GET /dispatch-runs` and every write path build scope from `@CurrentUser` and ignore the header. Never assume a surface honours acting without grepping `CurrentScope` in its controller  [auth-access, standing law]

## Refuted — never re-file (policy P17: rejected app-wide ⇒ C on sight)
- A ZONAL_MANAGER cannot widen scope with `X-Acting-As-Zone`, and a WM/SE cannot act at all: `zm.north` + header zone 2 stayed clamped to zone 1, `wm@fsm.test` got 403 at the role guard. Measured 2026-09-02. The acting breach (AA-01) is a missing *when*, never a missing *who* — do not re-file it as privilege escalation across roles  [auth-access, standing law]
- E-12 `inventory -> tickets` is NOT a blank receiver. `apps/mobile/src/navigation/screens/StockScreen.tsx:23,29,56,118` consumes `/me/component-requests` and posts confirm-receipt on SHIPPED rows. Refuted twice, independently (inventory + tickets)  [inventory+tickets, standing law]
- Availability/leave -> dispatch carrier EXISTS and is live-read, not a stale MV: `se-availability.service.ts:136` -> `currentStatusMany()` at `recommender.service.ts:1035` + `candidate-query.service.ts:122` -> drop at `candidate-readiness.ts:56`. The spine's omission was a map error (O4), not a broken hand-off  [engineers, standing law]
- Inventory rollback on failed verification IS built: `verification.service.ts:400-415` restores van stock, flips ROLLED_BACK, e2e-specced  [inventory, standing law]
- `@Public GET /api/non-op/confirm` is legitimate — one-time token is the credential, burned on use (`:286`)  [tickets, standing law]

## Fixture limits — measured, apply to every remaining module (cross-zone S3, 2026-09-02)
- **Every ticket in this DB is SILVER.** 6500 scanned via `GET /tickets?limit=500` paged as CSM: 0 PLATINUM, 0 GOLD. Any tier-gated capability (Platinum auto-escalation, Gold/Silver-only levers) is UNTESTABLE — return `UNTESTABLE` naming the tier fixture, never a pass from an empty result  [cross-zone, standing law]
- **No SE persona can receive an assignment.** `se.north@fsm.test` (user `2222…2222`) has no `engineerMaster` row and no zone-2 SE has a login, so `override.assignTicket` can never target the walkable SE. The SE end of every hand-off is UNTESTABLE by API, not "broken"  [cross-zone, standing law]
- **`zones.zonal_manager_user_id` does not point at the persona ZM.** `seed-mock-engineers.ts:109` aims it at `zm-z1@mock.fsm`, so any notifier that routes through that single pointer delivers to nobody you can log in as. Notifiers that resolve recipients BY ROLE (`usersInRoles`) do deliver. Judge a missing notification against which routing the code uses before filing it  [cross-zone, standing law]
- **A 500 mid-write is walkable evidence of a two-phase orphan.** Do not try to crash a server to prove a race — drive the second phase into a real error and read back whether phase 1 committed. cross-zone's approve did this six times unaided (`ev E2 → E4`)  [cross-zone, standing order]
- Server-side troubleshoot dedup WORKS. Same `clientSubmissionId` twice ⇒ `DUPLICATE` + the SAME `submissionId`, no second row (E4, walked). Never file "a retry creates two submission rows"; the client-side gap is the *unstable id*, whose real harm is a 409 naming a null winner  [tickets, standing law]

## Walk-enabling facts (2026-09-02)
- **The six seeded logins are `users` rows only.** `seedAuthFixtureUsers` writes no `engineer_master`
  and no `se_coverage`, so `se.north@fsm.test` is not an engineer and every SE queue reads 0. Unlock
  with `POST /api/org/engineers` then `POST /api/org/se-coverage` as OH (done for plant 103 in this
  DB; `/me/tickets` then returns 521). An empty SE queue is a fixture gap (O4), never product evidence  [tickets, standing law]
- **`componentUnavailable: true` on `POST /tickets/:id/troubleshoot` always returns HTTP 500** — CHECK
  `ts_submissions_component_unavailable_item` demands `component_unavailable_item`, which no caller
  populates. The component-request-to-warehouse hand-off is therefore unreachable over HTTP; the empty
  `/warehouse/requests` and `/me/component-requests` queues are a wall, not thin seed data. OWNED AND
  PRICED BY `tickets` as TKT-02 — inventory/warehouse must reference, never re-price  [tickets, standing order]
- `audit_logs` is the table (not `audit_log`); `GET /api/audit-trail/tickets/:ticketId` is the ZM read.
  It records TROUBLESHOOT_SUBMITTED, CROSS_ZONE_*, BATCH_OVERRIDE_* and SE_AVAILABILITY_SET, so a
  missing row there is a real omission, not a missing writer  [tickets, standing law]
- `static.md` STATUS-WRITE column false-positives: it matches the word `status` inside SELECT predicates, so pure read services (e.g. `dashboard.service.ts`, 22 `$queryRaw`, zero mutations) are reported as status writers. Killed one dashboard hypothesis that predicted an unaudited write on a read path. Verify a claimed write by finding the mutating call, never by this column  [dashboard, standing law]
- The Finance export is NOT over-permissioned. `GET /api/vouchers/export` as ZM -> 403 and as CSM -> 403; only OPERATIONS_HEAD gets 200. No ZM can pull another zone's money data. The gap at `vouchers.controller.ts:108` is that the pull is unaudited, not that it is over-scoped — never re-file it as a zone-leak  [vouchers, standing law]
- verification has ZERO live rows: `/verification/review`, `/verification/fraud-flags` and `/reports/verification-outcomes` all return 0/empty to every manager role. Check 3 (persist) is untestable module-wide and no empty verification list is ever evidence — 2-3 seeded `VerificationRun` rows across zones 1 and 2 unblock seven capabilities at once  [verification, standing law]
- role gating in verification is REAL and re-proved at E4 (WM and SE get 403 on review, fraud-flags, escalate and mark-auto-recovery) — the module's access defect is ZONE scope on `fraudFlags()` alone, never role scope. Never re-file "unguarded verification endpoint"  [verification, standing law]
- `GET /reports/fleet-uptime` returns `uptimePct: 100` on `eligibleDeviceCount: 0` — a perfect score from an empty denominator, reproduced E4 2026-09-02. Owned by `reports` (honest degradation), never priced by a consuming module  [verification, standing order]
- a zero-denominator metric that returns 100 is a fabrication, not a default: `uptimePct()` reports 100 on an unwritten month while its sibling `ratePct()` reports 0 — reproduced E4, the hero KPI reads 100% where the one computed month reads 56.19%  [reports, standing law]
- never take a number from a report called with DEFAULT parameters as evidence the report works — every monthly cube in this DB defaults to a month cron never writes; ask for `?month=2026-07`, the only month with real rows  [reports, standing law]
- reports zone clamp + role gate are REAL and honest at the API: `efficiency?zoneId=2` as zm.north echoes `filters.zoneId:1`, zm.south sees only zone 2, zm-scorecard is 403 for ZM and CSM. Refuted — never re-file a reports C6  [reports, standing law, policy P17]
- `exports.controller.ts:22` `dataAsOf` is the app's reference freshness contract; a summary table's `computed_at` column already exists (schema.prisma:2682) and the report projection drops it — price freshness as a select-list change, never as a schema or writer change  [reports, standing order]
- a DEFAULT on a retired enum value is not a C7 until live rows are counted: read the whole table through the API first — intraday's `PENDING_ACCEPTANCE` default holds 0 of 1936 rows, which demoted it from C7-on-live-data to S1 schema hygiene  [intraday, standing order]
- a dead endpoint's price depends on whether it is registered: 400-with-its-own-code as an authorised role means ALIVE ⇒ C4 (wire or delete, a decision); only a 404 makes it a free delete  [intraday, standing order]
- Dashboard reads are zone-clamped server-side exactly like `/tickets`, at ROW level, on all six `/api/dashboard/*` endpoints plus `/api/snapshots/latest`: zm.north and zm.south get disjoint single-zone payloads, CSM/OH get true pan-India aggregates (33 co / 340 plants / 29394 devices vs a ZM's 9 / 82 / 6560), WM and SE get 403 everywhere. `?zoneId=` narrows a CSM/OH and can never widen a ZM (activity-trend as zm.north with `?zoneId=2` still returned `zoneId:1`). Never re-walk the dashboard clamp  [dashboard, standing law, E4 2026-09-02]
- `SnapshotBanner` has NO age threshold — it alerts only on `latest.status==='FAILED'`, a RUNNING run past 15 min, or a gated ingestion streak (`SnapshotBanner.tsx:26,29`). A SUCCESS snapshot 21 hours old renders as the ordinary grey "data as of" line. Any module claiming its screen "degrades honestly" about staleness must check age separately  [dashboard, standing order]
- INACTIVE -> ACTIVE auto-recovery IS built and lives in `ticketing` (`auto-recovery.service.ts`, ticket status `CLOSED_AUTO_RECOVERY`, counted at `devices/device-detail.service.ts:117`) — a grep confined to the `ingestion` tree reads it as absent. Never file "a recovered device leaves a stale ticket" as a gap  [ingestion, standing law]
- the RUNNING backend is not built from the working tree: `GET /integration/health` returns no `ingestion` key because #300 (`ingestion-alert.ts`) is untracked and `health.service.ts:209` is uncommitted. Diff `git show HEAD:<file>` against the live payload before calling a missing field a gap — O4, never in the feature backlog  [ingestion, standing law]
- a run that finished SUCCESS is never "stuck": `SnapshotBanner.tsx:31` gates staleness on `status === RUNNING`, and no absolute-age rule exists anywhere, so an aged-out pipeline reports HEALTHY grey rather than unknown. When judging a freshness detector, ask what it reports on ZERO rows, not on bad rows  [ingestion, standing law]
- `audit_logs`'s `@@index([actedAsRole, actingZone, createdAt])` (schema.prisma:1733) IS read: `role-backup.service.ts:89` groupBy -> `GET /api/reports/csm-approval-share` -> a live admin page (`CsmApprovalSharePage.tsx`, AppRoutes.tsx:250, nav.ts:215). Never re-file "an index with no query behind it". The real gap is narrower: one AGGREGATE reader, no ROW-LEVEL reader  [notifications, standing law]
- a rendered control is not a working surface — `TopBar.tsx:212` ships an `aria-label="Notifications"` bell with no `onClick`, no fetch and no admin api client behind it, while the API holds real unread rows for CSM/OH. Check for a handler before crediting any shell affordance  [notifications, standing law]
- "N unguarded endpoints" from `inventory-static.mjs` means no `@Roles` only. Walked for notifications: all three are `401` anonymous and scoped by JWT subject (cross-user mark-read `404`, not `403`, so no existence leak). Report such counts as role-scope, never as open access  [notifications, standing law]
- the ROW-LEVEL audit reader is missing APP-WIDE, not per module: `/api/audit-trail` registers exactly ONE route, `tickets/:ticketId` (`audit-trail.controller.ts:20`), and grepping `apps/admin/src` for `audit-trail`/`auditTrail` returns NO hit at all. So every audit row that is not a ticket row is written and never readable by any operator. Price that reader ONCE at rollup; a module may only price the audit rows it fails to WRITE  [engineers, standing order, E4 2026-09-02]
- the engineers zone clamp is REAL on reads AND writes and is re-proved at E4 — out-of-zone `GET/PATCH /engineers/:seId` and `POST :seId/status` all 404 `SE_NOT_FOUND`, out-of-zone `POST :seId/availability` 403, out-of-zone leave approve 403 with an empty list, out-of-zone `DELETE /planner/:id` 403, SE-sets-another-SE 403, WM/SE `GET /engineers` 403. Never re-file an engineers/leave/planner C6  [engineers, standing law, policy P17]
- `se.north@fsm.test` (user `2222…2222`) has NO `engineer_master` row, so every SE-self path in engineers 404s and `/me/availability` returns 0 items. An empty SE availability or leave list is a FIXTURE artefact, never evidence about the feature — O4  [engineers, standing law]
- availability precedence is OLDEST-WINS, not last-write-wins: `currentStatusMany`/`currentStatus` (`se-availability.service.ts:60,88-94`) order by `windowStart desc` with NO tie-break, so two windows sharing a `windowStart` resolve in physical order and a manager's later clearing `AVAILABLE` write silently loses (reproduced 3/3). Any module reasoning about "the SE's current status" must assume the table can hold contradictions  [engineers, standing law, E4 2026-09-02]
- a bare `YYYY-MM-DD` on the leave and availability forms means an IST calendar day and lands as `18:30Z` of the previous day (`common/ist-day.ts`); `se_planner.plannedDate` is `@db.Date` and the Recommender keys it with the same `istDate()` helper. The IST-vs-UTC mismatch hypothesis is REFUTED — never price a "planner/leave date keying is broken" fix  [engineers, standing law]
