# S1 EXPECTED — tickets

**Persona chosen:** Ravi, Service Engineer (mobile only, 20+ stops/day). Lives in Ticket Detail
all day; every stop is a ticket state transition.

**Boundary as given:** admin /tickets, /install, /readiness/* + mobile ticket screens, one module.
Read-ban respected — derived from PRD lines 121-160, 232-258, 501-664 and CONTEXT.md decisions
4, 5, 8, 9, 11, 12, 13, 14. SPINE.md does not exist yet (spineAvailable: false) — cross-module
expectations (Recommender/Day Plan feed-in, Component Hard Filter, Fleet Uptime eligibility, Van
Stock) derived from CONTEXT.md decisions 5, 12, 13 instead.

**SCOPE contested — flagged, not resolved:** the module boundary fuses two personas. Ravi
executes; Zonal Manager separately owns the web oversight layer in the same boundary (ticket
list+drawer triage, WAITING_COMPONENT 7-day escalation, repeat-failure/ESCALATED flags,
Verification Review page, Non-Operational dual-confirmation, Recovery decision queue,
Vehicle-Unavailability review/resume, Component-Blocked Queue). PRD stories 20-41 back this as a
full second persona, not a thin wrapper. Recommend S2+ treat ZM-oversight as its own capability
cluster inside this module.

**Goals (Ravi):**
1. Execute assigned ticket work stop-by-stop (troubleshoot/install/recovery) with full context.
2. Handle field exceptions cleanly (component unavailable, vehicle unavailable, 409) without
   losing SLA fairness or component tracking.
3. Trust the closure/verification feedback loop without phoning the ZM.

**Capabilities:** C1-C11, see s1.json. 9 persona-derived, 2 role-derived, 0 spine, 0 prd-only.

**ANCHOR-CHECK: pass** — no capability sourced purely from PRD; all trace to Ravi's day or a role
consequence of it (409/QR).

**Notable domain rules baked into criticality calls (CONTEXT.md):**
- Decision 8: WAITING_COMPONENT is 1 cycle -> 1 ticket -> 1+ submissions, each with own
  `client_submission_id`; resubmit ownership depends on SE type + delivery destination + mandatory
  ZM confirmation (feeds C4).
- Decision 9: Phase-1 GPS check anchors on SE's form-submission GPS or ON_SITE geofence capture,
  skipped entirely when `presence_source = NONE` (feeds C3/C9).
- Decision 13: Shadow Use on 409 decrements van stock regardless of submission outcome (feeds C8).
- Decision 14: Non-Operational dual confirmation and Recovery-Ticket auto-creation on RECURRING
  deal type — ZM-side, called out in scopeNote, not folded into Ravi's C-list.

**Not read:** no source files, schema.prisma, AppRoutes.tsx, skeleton.md, static.md, browser — per
stage law.
