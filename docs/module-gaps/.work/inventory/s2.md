# S2 · ACTUAL — inventory (Components, Van Stock & Warehouse)

E2 static read. No browser. Checks 3 / 6 / 8 stay `?` where a walk is required. No `FUNC_COMPLETE`.

## Headline

| verdict | pointer |
|---|---|
| stock leaves van, never comes back on DISPUTE | `inventory/shadow-use.service.ts:77` vs decrement `ticketing/troubleshoot-submission.service.ts:330,375` |
| recovered device closes ticket, enters no stock ledger | `ticketing/recovery.service.ts:155` |
| notification spine claimed in a comment, wired nowhere | `component-request/component-request.service.ts:14` |
| 🟢 every write-verb endpoint audits | `warehouse-stock.service.ts:105` (`withAudit`) · `component-request.service.ts:203,269,337` · `shadow-use.service.ts:61,94` |
| 🟢 C7 rollback on failed verification IS built | `verification/verification.service.ts:400-415` + `test/inventory-rollback.e2e-spec.ts` |

## The two cartographer leads

**E-14 CONFIRMED — DANGEROUS.** A business-409 decrements the losing SE's van stock
(`troubleshoot-submission.service.ts:330` → `decrementStock` :375) and writes an `InventoryTransaction`
`SHADOW_USE`. WM Dispute (`shadow-use.service.ts:72-98`) writes status `DISPUTED`, a `reconciledBy`,
an `INVENTORY_DISPUTE` ticket event and an audit row — and no `seVanStock` write. Absence earned:
outside `src/generated`, only three files touch `seVanStock` at all — `inventory.service.ts` (reads),
the decrement, and `verification.service.ts:409` (the rollback upsert). No cron in either module dir.
Dispute means "the loser did not really consume this", so the van count stays permanently short.

**E-12 REFUTED.** The SE side does consume shipment state. `apps/mobile/src/navigation/screens/StockScreen.tsx:23`
loads `/me/component-requests` (`mobile/src/api/client.ts:246`) and renders a Confirm Receipt button on
`SHIPPED` rows only (:118), posting `confirm-receipt` (:255), with a test at
`StockScreen.test.tsx:88`. The skeleton missed it because it globbed `apps/mobile/src/stock/` only.
What is genuinely missing on that edge is the *push* — the SE must open the tab (INV-G3).

**C7 built.** `verification.service.ts:400` DEDUCTED on CLOSED; the else branch (:405-415) upserts
`seVanStock` back by `qty` and flips each row to `ROLLED_BACK`. This is the compensating write E-14 lacks.

## Skeleton / static corrections (do not carry these forward)

- `static.md` prints `...READ_ROLES` for `PATCH /api/inventory/warehouse-stock`; source is
  `@Roles(...WRITE_ROLES)` = WM + OH (`warehouse-stock.controller.ts:16,50`). Not a perms gap.
- skeleton says the four admin pages have **no data hook** and lists **zero** admin api calls. False —
  `admin/src/api/componentRequests.ts:32,63-66`, `api/shadowUse.ts:27,42-43`, `api/inventory.ts:32,60,72`
  are all wired into the pages (`ShadowUseQueuePage.tsx:45,50`, `WarehouseDashboard.tsx:50,95`).
- skeleton lists no page for `/warehouse/requests`; it is `ComponentRequestsPage` used twice —
  WM-actionable at `AppRoutes.tsx:422`, `readOnly` for managers at `:431`.
- C5 (zone stock levels) is on the **Warehouse dashboard**, not an inventory page
  (`pages/dashboard/WarehouseDashboard.tsx:50`), read + `apiSetWarehouseStock` write.

## Capability roll-up (11)

| id | state | why, in one pointer |
|---|---|---|
| C1 queue | PARTIAL | `warehouse.controller.ts:52` + `ComponentRequestsPage.tsx` |
| C2 approve/reject/ship | PARTIAL | `component-request.service.ts:147,157,219`; check 8 fails — no push |
| C3 shadow use | **BROKEN** | INV-G1 |
| C4 escalate to ZM | BACKEND_ONLY | audit-only escalation `shadow-use.service.ts:95`; ZM has no queue |
| C5 zone stock | PARTIAL | `warehouse-stock.service.ts:93,105` + WarehouseDashboard |
| C6 serial verification | NOT_STARTED | capture only at `install-lifecycle.service.ts:146` |
| C7 rollback | PARTIAL (built) | `verification.service.ts:405` |
| C8 7-day escalation | NOT_STARTED | `ageDays` display only; the 7-day flag is on the *blocked* queue `inventory.service.ts:96` |
| C9 recovery receipt | PARTIAL | closes ticket `recovery.service.ts:155`, no stock write (INV-G2) |
| C10 van stock mobile | PARTIAL | `StockScreen.tsx:23` + `commonKitStatus` `inventory.service.ts:51` |
| C11 low-stock alert | UI_ONLY | pill at `StockScreen.tsx:88`, no notifier |

## Left for S3 (walk)

Persist-after-reload on INV-G1 and INV-G2 (H1, H4); whether a SHIPPED request produces any
Notification row (H2); ZM/CSM role view of `/warehouse/shadow-use` and the PATCH write guard (H3).
