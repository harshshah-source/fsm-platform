# S2 · ACTUAL — verification (GPS Verification Review) · ZM persona · E2, no browser

## Lead settled: DEAD — page does consume its endpoints

`VerificationReviewPage.tsx:83` `useEffect` → `apiVerificationReview` (`api/verification.ts:73`,
plain `fetch` + sessionStorage bearer). Escalate `:96`, mark-auto-recovery `:107`. Truth (a).
No C3 false-completeness finding filed. Conductor's correction and my source read agree.

## Confirmations asked for

| claim | verdict | pointer |
|---|---|---|
| escalate audited + tx | 🟢 holds | `verification.service.ts:103` `$transaction` · `:112` ticketEvent · `:122` auditLog · `:96` zone scope |
| mark-auto-recovery audited + tx | 🟢 holds | `verification.service.ts:172` tx · `:190` ticketEvent · `:193` auditLog · `:196` retireAssignmentOnClosure |
| C3 distance delta shown | 🟢 built | `VerificationReviewPage.tsx:282` `N m off` from `firstPingDistanceMeters` |
| C1 four outcomes on one page | 🟢 built | `verification-query.service.ts:56` `rowTypeFor` → 6 row types; chips `:273–:292` |
| C7 outcome → reports ledger | 🟢 exists, **O1 refuted** | `reports.service.ts:549` + `system-efficiency-aggregation.service.ts:100` read `verification_runs` **by raw SQL alias `vr`** — a Prisma-name grep misses them |

C7's real risk is not O1 but **P3**: two ledgers read two different columns —
`system-efficiency:100` counts `vr.outcome`, `fleet-uptime:92` counts `ticket.status` — and V-01
makes those columns disagree on the same closure.

## Worst finding — V-01, and it is a two-line read

`VerificationReviewPage.tsx:225` offers *Mark auto-recovery* on `PARTIAL_RECOVERY` **and**
`FAILED_NO_PINGS`. But `rowTypeFor` (`verification-query.service.ts:56`) only returns
`FAILED_NO_PINGS` when `outcome === 'FAILED_VERIFICATION'` — non-null. And the write is
`verificationRun.updateMany({ where: { ticketId, outcome: null } })`
(`verification.service.ts:186`) — **zero rows matched**. The ticket still flips to
`CLOSED_AUTO_RECOVERY` (`:181`). Result: run says failed, ticket says auto-recovered, permanently;
the row re-renders unchanged so the button looks inert; and the two report aggregations above
count that one closure two different ways. Fraud/credit-integrity class ⇒ DANGEROUS.

## Second: the fraud-flags surface is both unscoped and unused

`verification-query.service.ts:214` `fraudFlags()` takes **no scope argument**;
`verification.controller.ts:111` injects **no `@CurrentUser`** — alone among the five handlers.
Guard is `MANAGER_ROLES`, which includes `ZONAL_MANAGER`, so a ZM reads every zone's fraud flags.
`review()` (`:173`) and `forTicket()` (`:115`, fixed by #162) are both scoped. This is the same
class of miss #162 closed, one route over. Separately: grep `fraud-flags|fraudFlags` over
`apps/admin/src` + `apps/mobile/src` → **zero**. Endpoint is e2e-tested
(`verification-controller.e2e-spec.ts:211`) and reachable by no screen. Both findings stand on
their own greps, not on the scanner signal.

## Countdown is decorative past the deadline

Displayed rule `startedAt + 24h` (`verification-query.service.ts:206`). Enforced rule is 24h
**AND** telemetry watermark advanced (`verification.service.ts:240`). The service comment at
`:235` says the stalled-watermark case "must be made visible (slice 3)" — slice 3 is not built,
and `VerificationReviewRow` (`api/verification.ts:23`) carries no staleness field, so the page
cannot show it. ZM gets a permanent unexplained `overdue`.

## Absence earned

C3's fraud-flags list: route search ✓ · backend search ✓ · role check ✓ (MANAGER_ROLES, not
hidden) · terminology ✓ (`fraudFlag`, `firstPingDistanceMeters`, `FAILED_FRAUD` all searched) ·
other-module entry point ✓ (mobile `VerificationScreen.tsx:58` is badge-only, read-only, no delta).
V-04 un-escalate: same five, `un-escalate|unescalate|deescalate` → zero.

## O4 — dev-process, NOT feature backlog (per conductor)

- `inventory-skeleton.mjs` reports "no data hook" for admin pages that fetch with
  `useEffect` + a typed `apps/admin/src/api/*` client. False here, and on all four engineers
  pages and the inventory pages. Also emitted an empty "admin api client calls" section while
  `apps/admin/src/api/verification.ts` exists. Scanner should match `apps/admin/src/api/` imports,
  not react-query only.
- `inventory-static.mjs` audit column follows one delegation hop, so it under-reports audit
  (11/11 false on scheduling, 8/10 on tickets). Direction is one-way: a reported "yes" is
  trustworthy, a "no" is not. Both verification "yes" values verified at file:line above.

## States

C1 PARTIAL · C2 PARTIAL (check 5 = x) · C3 PARTIAL (check 6 = x) · C4 PARTIAL (check 8 = x) ·
C5 PARTIAL · **C6 BROKEN** · C7 PARTIAL. PARTIAL = built-but-unwalked unless a check is `x`;
checks 3/6/8-by-walk stay `?` — no browser in S2, so no FUNC_COMPLETE was awarded.
