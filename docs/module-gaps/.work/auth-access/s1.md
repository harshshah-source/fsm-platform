# S1 — EXPECTED — auth-access

Read: PRD-fsm-admin-dashboard.md:52-88 (role matrix + banner rule :62). CONTEXT.md §15 (802-814,
role hierarchy + backup cascade + acted_as_role) and :507 (banner). SPINE.md edge table (0 edges
name auth-access — auth is cross-cutting infra, not a data-flow edge; expected, not a gap).
.scratch/fsm-platform-v1/INDEX.md grep (intent only): #91 (Postgres credential store, in-memory
store still live per that grep), #110 (login/refresh rate limiting), #133 (per-zone ZM logins,
superseded by #91), #162 (SE row-level authz floor), #27 (role backup cascade + CSM acting scope,
accepted core), #47 (RequestActor acted_as_role seam), #109 (admin session lifecycle).

## Persona
Central Service Manager — cross-zone oversight + acting-scope backup. Frequency: acts as ZM
backup whenever a ZM is unavailable (leave / marked-unavailable / >24h heartbeat auto-activate) —
per CONTEXT §15 this is routine, several times a week, not an edge case.

## Goals (≤3)
1. Authenticate and hold a session across a day spanning own-scope + acting-scope work.
2. Enter acting-scope for an unavailable ZM's zone with an unambiguous live-authority indicator.
3. Have every acting-scope action attributed (`acted_as_role`) so audit + %-backup report hold up,
   then exit cleanly.

## Scope note
Module holds two distinct experiential gates: CSM mid-session role-switch (acting scope) vs
Ravi/SE mobile day-start login. Picked CSM because P4 (separation-of-duties) and P2 (no audit)
findings live in the acting-scope mechanism specifically, not plain login. SE mobile login is
in-scope per brief but is a different persona's day — not scored here; S2 still owes mobile auth
evidence.

## Capabilities
| id | name | src | crit | why |
|---|---|---|---|---|
| C1 | Credential login issues a session | persona | 5 | day can't start, own or acting scope |
| C2 | Session survives full day, no silent logout | persona | 4 | drops covered ZM's zone mid-task |
| C3 | Enter acting-scope for unavailable ZM's zone | persona | 5 | this IS the backup cascade (§15) |
| C4 | Persistent "Acting as ZM for [Zone]" banner | prd | 4 | no live-authority indicator ⇒ P4 risk |
| C5 | `acted_as_role` stamped on every acting action | prd | 5 | unattributed acting = unauditable (P2) |
| C6 | Clean exit from acting-scope | persona | 4 | stale elevated access = standing P4 |
| C7 | Role-scoped nav (only permitted pages render) | role | 4 | wrong-role page visibility is a breach itself |
| C8 | Auto-activate acting-scope on >24h heartbeat gap | role | 3 | unnoticed ZM absence silently stalls zone |
| C9 | Login rate limiting / brute-force protection | role | 3 | single gate for all 5 roles, account-takeover exposure |

## Anchor-check
**pass** — srcMix persona 4, role 3, spine 0, prd 2. Not prd-echo; derived from persona's actual
day and the role/backup-cascade model, PRD only backs the banner text and audit-field name.

SPINE note: zero edges tag `auth-access` — consistent with auth being enabling infra every other
module calls through, not a producer/consumer edge in the work-crossing graph. Not treated as a gap.
