# S1 — Expected: cross-zone (Cross-Zone Escalation)

Persona: **Central Service Manager**. Freq: daily escalations + backup cover for absent ZMs.

## Goals (≤3)
1. Work the escalation queue (Auto/Platinum vs Manual/Gold-Silver): approve / deny / defer.
2. Act with full ZM authority when covering an absent ZM's zone.
3. Confirm an approved hand-off actually reaches the receiving zone's SE, not just a status flip.

## Capabilities (8)
| id | name | src | crit |
|---|---|---|---|
| C1 | Platinum auto-escalation (1h CRITICAL / 4h SUBMITTED) fills CSM queue | persona | 5 |
| C2 | Approve → commits cross-zone hand-off (target zone + SE) | persona | 5 |
| C3 | Deny/defer → reverse path, ticket back to home ZM queue + reason | persona | 5 |
| C4 | ZM manual flag (Gold/Silver) → lands in CSM queue | role | 4 |
| C5 | Denied-Platinum re-escalation, home ZM → Operations Head | spine | 3 |
| C6 | "Acting as ZM for [Zone]" banner + `acted_as_role` audit tag | role | 4 |
| C7 | Approved escalation actually delivers to target-zone SE worklist | spine | 5 |
| C8 | Per-zone "% approvals by CSM" report for Ops Head | prd | 2 |

ANCHOR-CHECK: **pass** — src mix persona 3 / role 2 / spine 2 / prd 1, not prd-only.

## Notes
- This module is a hand-off: every capability above either crosses the zone boundary (C1,C2,C7) or
  is the reverse/failure path when the crossing doesn't happen (C3,C5). C7 is written explicitly
  because SPINE's E-24 flags the target-zone SE leg as riding on a separately-broken push path — the
  yardstick treats "approved" and "delivered" as two different capabilities on purpose.
- SCOPE note: Cross-Zone Dashboard page itself is CSM/OH only (role matrix). ZM's flag action lives
  in the ZM's own Ticket List / "Couldn't Assign" queue (outside this module's page), but its landing
  effect is in-scope here — recorded as boundary-adjacent, not resolved.
- No PRD tier/priority line range (:781-819) actually defined Platinum/Gold/Silver — that range was
  general terminology-avoidance table. Tier definition + priority order came from CONTEXT.md
  (`company_tier`, Platinum > Gold > Silver) and issue #32's disposition, not the given PRD range.
- SPINE.md present (spineAvailable: true) — edges E-23 (cron sweep → cross-zone), E-24 (approve →
  in-process assign, already flagged PARTIAL/S3 for missing reverse), used directly.
- Sources read: PRD :161-168, :52-88, :781-819; CONTEXT.md (grep: cross-zone/escalat/tier terms);
  SPINE.md (grep: cross-zone/escalat); issues #32 (cross-zone-escalation), #27 (role-backup-cascade).
