# FE-21 — Reports landing (Fleet Uptime + Soft-Inactive) · 2026-06-29

**Status: done.** Commit `878aa68` on `feat/FE-21-reports`. Single clean feature commit (10 files).

## What shipped (ref `21`)

`/reports` landing, manager-gated, all panels from real backend endpoints:
- 6-up KPI `MetricStrip` (Fleet Uptime, Total Inactive, Critical+, Eligible Devices, Auto-Recovered, SE-Repaired)
- Inactivity-by-SLA-bucket `BarChartCard`
- Fleet Uptime % monthly `TrendChart` (Issue 39 — monthly fan-out of the single-month endpoint)
- Soft-Inactive count trend (Issue 40 — Operations-Head-gated; other roles see a gated panel)
- Fleet Uptime % by zone bars + Zone-breakdown `DataTable`
- empty / loading / error states throughout
- typed `api/reports.ts` client; `ReportGrid` composition primitive introduced; Analytics→Reports nav + route

## Acceptance criteria

- [x] Reports landing matches `21` (KPI strip + bars + zone-breakdown table) — **donut/work-type gated → #90**
- [x] Fleet Uptime % monthly TrendChart from Issue 39
- [x] Soft-Inactive count trend from Issue 40
- [x] All metrics from real endpoints; empty/loading/error states
- [x] Reusable `ReportGrid` introduced
- Playwright ≈ `21` → deferred to FE-00 (repo-wide visual-regression harness)

## TDD cycle

- **RED:** the existing `reports.test.tsx` failed — `getByText('Fleet Uptime')` was ambiguous (KPI label *and* Zone-breakdown column header).
- **GREEN:** disambiguated the assertion (unique KPI hint + `getAllByText`); `ReportGrid` test-first then created.
- **REFACTOR:** ReportsPage uses `ReportGrid` for its three panel rows; mock Export button tidied to a disabled Export; gated-panel copy points at #90.

## Verification (isolated HEAD + FE-21 tree)

- `tsc --noEmit`: clean.
- FE-21 tests: `reports.test` (2) + `report-grid.test` (1) — stable green.
- `vite build`: clean (pre-existing chunk-size advisory only).
- Note: the full admin suite shows **1 pre-existing flaky failure** (a *different* test each run — `component-blocked` / `vouchers-review`; both pass in isolation). Unrelated to FE-21 — a test-isolation issue in the existing suite (cf. Issue 50). Flagged, not introduced here.

## Cleanup / separation

- Removed debug/preview artifacts (backed up to scratchpad): `__shot.mjs`, `__shot2.mjs`, `apps/admin/preview.html`, `apps/admin/src/preview.tsx`.
- The **shell/sidebar refactor** (collapsible sidebar + `SidebarContext` + `BrandLogo` + TopBar/AppShell/Footer/icons/index.css + `sidebar-shell.test.tsx`) is **not part of FE-21** and was **not required** for it (FE-21 runs against the HEAD shell). Left uncommitted in the working tree, separated from this commit — **it needs its own issue + commit** (currently unowned scope). Backup patch: `scratchpad/fe21-backup/shell-refactor.patch`.

## Follow-up filed

- **#90** — backend Work-type-mix + Verification-outcome aggregation endpoints (lights up FE-21's two gated panels).
