# S1 — dashboard (Zone Dashboard Home)

Persona: **Zonal Manager**, own zone, all day every day, default landing page on login.
spineAvailable: true (SPINE.md exists, A1 cartographer already ran).

## Goals (3)
1. Triage Action Required same-day (8 card types, urgency-ordered).
2. Read zone health at a glance, spot deterioration (Zone/Company overview, SLA split, trend%).
3. Act on worst devices fast (Grouped Critical Work Queue, one-click assign).

## Capabilities — see s1.json for full fields
C1 Action Required panel · crit5 · persona
C2 Zone Overview (SLA split + trend%) · crit5 · persona
C3 Company/Plant drill-down · crit4 · persona
C4 Grouped Critical Work Queue + one-click assign · crit5 · persona
C5 Snapshot data-as-of banner · crit4 · persona
C6 Zone operating-mode (DEFICIT/PREVENTIVE) plain-language · crit3 · persona
C7 Fleet activity trend chart · crit2 · prd
C8 Excel export · crit3 · persona
C9 Role-scoped lens (ZM/CSM/OH differ) · crit4 · role

ANCHOR-CHECK: pass. srcMix persona7/role1/spine0/prd1 — not prd-echo.

## SPINE cross-check
Only one dashboard-touching edge in SPINE.md: **E-07** — SE soft-state ping (`POST /tickets/:id/soft-state`,
`/me/activity-ping`) receives into `/` zone-operations · ZM, marked OK. No inbound edges found for
Component-Blocked Queue, Non-Op AWAITING_MANAGER_CONFIRMATION, or Cross-Zone Escalation feeding
Action Required — either SPINE's A1 pass didn't trace those hand-offs yet, or they're a gap. Not
resolved here — flag for S2/S3 to check against actual card wiring.

## Scope note
PRD page-inventory (:321) names dashboard = Action Required + Zone/Company tables + Grouped Critical
Work Queue only. Flow 1 (:347-478) additionally requires the snapshot banner up top and (via cross-ref
to #134/#136 intent) fleet-activity-trend + zone operating-mode visuals live in this page body too.
Action Required cards are entry points into other modules (Schedules, Intraday, Component-Blocked,
Non-Op queue) — this module's job is surfacing + routing, not owning those workflows downstream.

## Role-lens flag (taxonomy C6)
PRD flows describe ZM own-zone scope throughout Flow 1. CONTEXT.md: CSM has full acting-authority
substitution for an unavailable ZM (acted_as_role audit) plus "current-phase" fleet-wide breadth; OH
is always fleet-wide read + configurator. A dashboard rendering the *same* Zone Overview/Company
tables to all three without an explicit acting-scope indicator (CSM) or fleet aggregate framing (OH)
fails role-lens — flag for S2 (does the built UI branch by role) and S3 (severity).

## Issue-intent notes (used for intent only, not status)
#134 fleet-activity-trend — chart intent: Inactive vs Troubleshoot vs Install, 1D/7D/1M/1Y/MAX,
Pan-India + zone-wise, placed between KPI hero and SLA Distribution.
#136 zone-operating-mode-visibility — DEFICIT/PREVENTIVE + plain-language reason to ZM (own zone) and
OH/CSM (cross-zone), UI-visibility only, reuses existing engine signal.
#143 critical-devices-kpi-regression — names a "Critical Devices KPI" hero card + "SLA Distribution"
as dashboard-adjacent artifacts implied by PRD flow, worth checking S2 for presence.
