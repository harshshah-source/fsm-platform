# S1 · EXPECTED — admin-config

Persona: **Operations Head** (weekly review + ad-hoc config changes). CSM co-owns exactly one dial
(SE assignment threshold, C6). No separate Admin persona exists (CONTEXT.md:934) — OH IS the
system configurator, so this module holds every dial that isn't itself a workflow queue.

## Goals (3)
1. Tune the dials that govern dispatch/scoring fleet-wide without corrupting historical KPIs or
   breaking in-flight work.
2. Administer accounts for every non-OH role.
3. Prove/audit any figure or config change back to source rows or actor.

## Scope note
PRD:345 draws admin-config as one `/settings` page. Code reality disagrees: plant deactivation
(SPINE E-26), SE assignment threshold (issue 238), and Ops Explorer (issue 217) are each their own
route/controller, reachable outside the OH-gated Settings nav in at least one case (CSM route for
C6). Real boundary = "every screen only OH can turn a dial on", not the `/settings` URL. Not
resolved here — flag for S2/S3 to check both the `/settings` page and these satellite routes.

## Capabilities

| id | name | src | crit | why |
|---|---|---|---|---|
| C1 | Zone/plant/SE-mapping config | persona | 5 | Recommender routing keys off this directly; wrong mapping mis-routes a whole zone |
| C2 | SE coverage type + territory (dedicated/multi-plant/floating, hierarchical+polygon) | persona | 4 | precedence chain (CONTEXT.md:590) has nothing to key off without it |
| C3 | SLA rules per device bucket/company tier | persona | 5 | drives every SLA badge + escalation timer fleet-wide; contractual exposure |
| C4 | Company Tier/Priority (OH global) + zone-scoped expiring overrides (CSM/ZM) | role | 5 | top key of canonical sort (Decision §17); override split is separation-of-duty by design |
| C5 | Common Kit definition + scoring weights | persona | 4 | Hard Filter/scoring untunable without it |
| C6 | SE assignment threshold, OH/CSM co-owned, OH lock+revert | role | 5 | gates ticket creation AND auto-dispatch fleet-wide (issue 238) |
| C7 | Plant deactivation/reactivation, dependent-work handling | spine | 5 | SPINE E-26: strips live SE stops, no outbox row, SE never told, reactivate doesn't restore — BROKEN S4, P1/P4 |
| C8 | User account management (ZM/CSM/WM) | role | 5 | only provisioning path in the system (CONTEXT.md:934); day stops without it |
| C9 | Audit trail on every config dial | role | 5 | every dial here moves dispatch volume or money — P2 class per taxonomy |
| C10 | Ops Explorer (trace figure to rows, flag-gated OH-only) | prd | 3 | issue 217, off by default, dispute-resolution tool not daily driver |
| C11 | Build health monitoring | prd | 2 | passive weekly signal; SPINE E-25 says it under-shows (no device-departure export visibility) |

## ANCHOR-CHECK: pass
srcMix persona=4 role=4 spine=1 prd=2. Not all-prd.

## Notes for S2/S3
- C7 (plant deactivation) already has hard evidence of BROKEN from SPINE — highest-value place to
  start code-side verification.
- C9 (audit) is cross-cutting — check per-capability, not as one screen.
- C4's override mechanism (issue 157/PRD:178) is a P4-relevant separation-of-duty feature — verify
  the override is actually zone-scoped + expiring + audited in code, not just documented.
- Satellite routes to check alongside `/settings`: plant-deactivation flow, AssignmentThresholdPage
  (CSM-reachable), `/ops-explorer`, `/build-health`.
