# S2 · ACTUAL — tickets (Tickets & Field Work Lifecycle)

11 capabilities scored. 1 NOT_STARTED, 1 BROKEN, 9 PARTIAL. No FUNC_COMPLETE — static read only.
45 endpoints, 30 mobile/admin files, 19 spine edges. Browser ban honoured: checks 3/6/8 stay `?`.

## static.md corrections (both flags verified against source)

| static.md claim | verdict |
|---|---|
| 10 write endpoints with no audit | **8 false positives.** recovery reschedule/close-failed/escalate/manual-close all audit via the shared private helpers `transition()` recovery.service.ts:373 and `closeWith()` :336. install create/upload audit at install.service.ts:114,272. auto-recovery-close audits at auto-recovery.service.ts:354. |
| — | **2 REAL.** `POST /api/vehicle-unavailability` (vehicle-unavailability.service.ts:140) and `POST /api/vehicle-unavailability/:id/resume-sla` (:434) both move the SLA clock inside a bare `$transaction` with no auditLog write. The only audit in that file is :374. ⇒ TKT-01, P2, DANGEROUS. |
| 1 `@Public` endpoint | **Legitimate by design.** `GET /api/non-op/confirm` non-operational.controller.ts:131 — the one-time token IS the credential, burned on use (non-operational.service.ts:286), 404/410/409 discriminated. Rate limiting deferred to #110 by its own comment. Not a finding on its own. The finding beside it is TKT-09: the notifier that mails the link is `LoggingCustomerConfirmationNotifier` (customer-confirmation-notifier.ts:26) — the customer never receives the link. |
| E-12 `ComponentRequest SHIPPED → mobile` = **blank receiver, S4** | **WRONG — receiver exists.** StockScreen.tsx:29,56,118-122 calls `apiGetMyComponentRequests` and renders a `confirm-receipt-*` button on SHIPPED rows. Downgrade E-12 from BROKEN. |

## ZM cluster — the asymmetry runs the other way

The scout was right that a second persona lives here; it is wrong that the ZM side is weaker.
Every ZM endpoint in static.md has a wired admin consumer: TicketsPage.tsx:81,119 (filters +
special-verdict count), TicketDetailDrawer.tsx:113-145 (Assignment History / Forms / Verification
tabs, lazy per tab), RecoveryDecisionQueuePage.tsx:45-81 (all three decision actions),
NonOperationalQueuePage, VehicleUnavailabilityPage. **Not a C4 finding.**
The unreachable and unvalidated paths are all on Ravi's mobile side (TKT-02..05). Record that
inversion — the surfacing rule's usual direction does not hold in this module.

## Worst of what code alone settles

- **TKT-03 / H-TKT-1** — `Crypto.randomUUID()` sits *inside* `handleSubmit`
  (TroubleshootFormScreen.tsx:56). The backend's dedup key is `seId_clientSubmissionId`
  (troubleshoot-submission.service.ts:113) and it works — but every retry mints a new key, so the
  one mechanism protecting root-cause analytics from poor plant connectivity is defeated by the
  client. No offline queue exists anywhere in `apps/mobile/src/tickets`.
- **TKT-05** — the same handler swallows every non-409 error (:66-69). Install
  (InstallFormScreen.tsx:89) and Collection (CollectionFormScreen.tsx:53) both render an error
  banner; the troubleshoot form, the one submitted 20+ times a day, renders nothing. The SE cannot
  tell a dropped submit from a successful one — which is also what makes TKT-03 fire.
- **TKT-02 / H-TKT-2** — the component-unavailable toggle is a bare boolean
  (TroubleshootFormScreen.tsx:37). `componentUnavailableItem` is declared (service :37), written
  (:231) and never populated: no picker on mobile, not in the controller body map
  (troubleshoot.controller.ts:26). SLA pauses correctly (:207-221); the WM receives a request with
  no part identity.
- **TKT-01 / H-TKT-3** — see the correction table. An SLA pause is compliance evidence; two of the
  four routes that move it leave no actor trail.
- **TKT-04 / H-TKT-4** — ConflictScreen.tsx:32 renders a Shadow Use reassurance line that cannot
  fire; its own docstring (:17-19) admits `shadowUseRecorded` is permanently false over HTTP.
  Pairs with E-14 (dispute never restores the decremented van stock) — same missing wire.

## Strong, do not re-litigate

C5 install (mandatory GPS+SIM serials, server-validated against `ticket.deviceId`, one-tx
FITTED→ACTIVATED with audit, install-lifecycle.service.ts:123-158) and C6 recovery
(INVALID_SERIAL / NOTES_REQUIRED at recovery.service.ts:120-131, reason-coded unable-to-collect
into the ZM queue) are the two best-built lifecycles in the module. C9 verification visibility
renders all four verdicts (VerificationScreen.tsx:26-29). C1 detail is genuinely rich —
transporter contact, SLA bucket, failure-cycle walk, expected components, telemetry.

## Cannot map

None. All 11 s1 capabilities located in source.

## Free for S3 to settle in one walk

File a VU report, approve it, resume it — then read `audit_log` (settles H-TKT-3) and reopen the
ticket detail (settles the C1/C2 persist `?`). Submit a troubleshoot form twice with the network
dropped between taps (settles H-TKT-1). Open a shared-pool RECOVERY ticket as an unassigned SE
(settles H-TKT-5).
