# Handoff — 2026-06-25 · continue issue building

> **ARCHIVED 2026-07-12 — consumed session handoff.** Current state lives in `docs/SYSTEM-STATE-2026-07.md`; work tracking in `.scratch/fsm-platform-v1/INDEX.md`. Historical record only.
Fresh-agent handoff for the FSM GPS platform (greenfield). This doc only captures what is **not**
already written elsewhere. Read these first, in order:

1. `CLAUDE.md` + `CONTEXT.md` (domain authority)
2. `docs/agents/workflow.md` (AFK HITL policy, per-slice TDD report, parity gate)
3. `.scratch/fsm-platform-v1/INDEX.md` — **the live backlog truth** (status per issue)
4. Recall the auto-memory index `MEMORY.md` (PG16/5433 dev DB, AFK-TDD auto-proceed, etc.)

## Where things stand (this session)

Six issues built+committed on branch **`feat/issues-28-31-45-46-49-62`** (pushed to origin; 6 commits
ahead of `main`). Per-issue progress docs already exist — do not re-summarise them here:

- `docs/progress/{28,31,45,46,49,62}-*.md` — each has AC table, slice log, deviations, parity disposition.

Commits (one per issue): 62, 49, 46, 45, 28, 31 (see `git log main..HEAD`). 28 & 31 are
**accepted-core** (mobile/sub-ACs deferred to filed follow-ups 64/65/66); 45/46/49/62 fully closed.

**Verified green at HEAD:** backend 34/34 on the new specs (full suite was 486), admin 15/15 (full 79),
both `tsc --noEmit` clean. Full-suite numbers were observed mid-session, not re-run at final HEAD — if
in doubt, re-run before further commits.

### Open loop
- **PR not opened** — `gh` CLI is not installed and no `GH_TOKEN` in env. Branch is pushed; PR body is
  ready at the OS temp scratchpad (`…/scratchpad/pr-body.md`) and was printed in the transcript. To
  finish: install GitHub CLI + `gh auth login`, or set `GH_TOKEN`, then `gh pr create`.

## What to do next (recommended)

Full ranked analysis is in the transcript; the short version — continue on the backend/admin stack with
the **P6 operational line**, in this order:

> **#47 (RequestActor acting-attribution seam) → #35 (Non-Op dual-confirmation) → #36 → #37**,
> then **#33 (Install Ticket create) → #34**.

Rationale: largest untouched operational area, all deps met. **#47 is overdue** ("land before
27/33/35"; 27 already shipped) — do it first so Install/Recovery creation+closure audits attribute the
acting-role correctly. **#35's only blocker (`deal_type`, #49) was cleared this session.**

Also unblocked AFK if you prefer reporting: **#39/#40/#41/#43/#44** (deps met). **#44** also closes
#49's deferred admin tag control. Small cleanup: **#50** (test-isolation flaky — observed mid-session).

**Blocked — do NOT start (need a human / a gate):**
- `#03` Notifications spine (HITL: FCM/APNs/WhatsApp creds+templates) → gates 29/30/32/42/53.
- `#54` Mobile Foundation (AFK but big RN/Expo lift) → gates the entire M-series + mobile follow-ups
  (52/63/64/66). Mobile half is 0% built; #54 is the single highest-leverage unlock when you choose to
  start the SE app.
- `#51` / `#65` are buildable but under-specified (no #51 spec file; #65 needs the AutoPlant external
  seam). #65's AC#6 authority conflict is already **resolved** (field-path, CONTEXT-aligned) — see its
  follow-up file.

## Repo gotchas (cost real time if unknown)

- **Bash tool cwd drifts** between calls (often resets to `apps/backend`). Always `cd` explicitly or use
  `git -C <repo>` / absolute paths. Admin tests must run from `apps/admin` (different vitest include).
- **Bash, not PowerShell** in the Bash tool — do NOT use PowerShell `@'…'@` heredocs for commit
  messages; use `git commit -F <file>` with a message file (avoids `( )` / quoting breakage).
- **`docs/*` is gitignored** (only `docs/agents/` tracked) — so `docs/progress/*.md` (incl. this file)
  are **local-only**; never `git add -f` them. Tracked planning lives in `.scratch/fsm-platform-v1/`.
- **`git add -p` / interactive staging is unavailable** — whole-file staging only. Plan commits so
  shared files (`app.module.ts`, `AppRoutes.tsx`, `AdminShell.tsx`, `INDEX.md`) land in one commit.
- LF→CRLF warnings on `git add` are benign (autocrlf).
- Tests are real-DB e2e (PG16+PostGIS on **5433**) — they hit the dev database; keep the NS/cleanup
  pattern from existing specs to avoid cross-test contamination (known flaky: #50).

## Working conventions (confirmed this session)

- Strict TDD via `/tdd` (RED→GREEN→REFACTOR, one test at a time; never refactor while RED).
- After each issue: write `docs/progress/NN-*.md`, update its `INDEX.md` line, file follow-ups for any
  deferred in-scope UI/mobile AC (parity gate — never silently defer; "no app shell" is NOT a valid
  reason, that's a #54 dependency).
- Reuse existing engines rather than forking (e.g. #31 added an optional `auditAction` param to the
  shared `override.service.ts` with defaults preserved — verify no regression in the override/critical
  suites when touching it).
- AFK: keep going; stop only for architecture / business-rule conflict / backlog-ownership /
  external-access / security (per `docs/agents/workflow.md`).

## Suggested skills for the next session

- **`/tdd`** — drive every slice (the required protocol here).
- **`/code-review`** or **`/review`** — run before committing a batch (review since `main` or the
  merge-base) to catch correctness/altitude issues.
- **`/field-ops-director`** — sanity-check #35/#33 (Non-Op / Install) against field reality before/after
  building; this is exactly the operational domain it reviews.
- **`/triage`** — if #51's missing spec or any new gap needs an issue authored before building.
- **`handoff`** — at the end of the next session.
